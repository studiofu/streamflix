server:
  port: 8085

spring:
  application:
    name: admin-service

eureka:
  instance:
    prefer-ip-address: true
  client:
    registry-fetch-interval-seconds: 5 # Fetch updates quickly
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
