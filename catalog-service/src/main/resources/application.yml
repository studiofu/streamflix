server:
  port: 8081

spring:
  application:
    name: catalog-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/streamflix_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      